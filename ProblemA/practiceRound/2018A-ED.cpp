#include <iostream>
#include <cmath>
#include <cstdlib>
#include <map>
#include <string>
#include <algorithm>
using namespace std;

map<int, string> Digits;
int t, n;

int main()
{
    cin >> t;
    for (i = 1; i <= t; i++)
    {
        cin >> n;
        int c = int(log10(n) + 1);
        for (int e = 1; e <= c; i++)
        {
                }
    }
}